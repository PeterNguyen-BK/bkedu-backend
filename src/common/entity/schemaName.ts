const userSchemaName = 'User';

const classSchemaName = 'Class';

const studentSchemaName = 'Student';

const teacherSchemaName = 'Teacher';

const subjectSchemaName = 'Subject';

const quizSchemaName = 'Quiz';

const questionSchemaName = 'Question';

const resultSchemaName = 'Result';

export const schemaName = {
  userSchemaName,
  classSchemaName,
  studentSchemaName,
  teacherSchemaName,
  subjectSchemaName,
  quizSchemaName,
  questionSchemaName,
  resultSchemaName,
};
